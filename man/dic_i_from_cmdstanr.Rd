% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmdstanr.R
\name{dic_i_from_cmdstanr}
\alias{dic_i_from_cmdstanr}
\title{Extract DIC_i from a CmdStanR fit object}
\usage{
dic_i_from_cmdstanr(fit, log_lik_name = "log_lik")
}
\arguments{
\item{fit}{A \code{CmdStanMCMC} object from \code{\link[cmdstanr:model-method-sample]{cmdstanr::sample()}}.}

\item{log_lik_name}{Character string giving the name of the log-likelihood
variable in the Stan model's \verb{generated quantities} block. Default
\code{"log_lik"}.}
}
\value{
A \code{DICi} object (see \code{\link[=compute_dic_i]{compute_dic_i()}}).
}
\description{
Convenience function that extracts the pointwise marginal log-likelihood
matrix from a \link[cmdstanr:cmdstanr-package]{cmdstanr::cmdstanr} fit object and computes DIC_i.
}
\details{
This function requires that:
\enumerate{
\item The \code{cmdstanr} and \code{posterior} packages are installed.
\item The Stan model includes a \verb{generated quantities} block that computes
\strong{marginal} pointwise log-likelihoods stored in an array named
\code{log_lik_name}.
}

The function extracts the log-likelihood draws as an S x N matrix using
\code{fit$draws()} and passes it to \code{\link[=compute_dic_i]{compute_dic_i()}}.
}
\examples{
\dontrun{
library(cmdstanr)
fit <- mod$sample(data = stan_data, chains = 4, iter_sampling = 1000)
dic_i_from_cmdstanr(fit)
}

}
